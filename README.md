# Hash_Finder
Hashfinder is a web app that securely hashes files and generates unique fingerprints using various cryptographic algorithms including MD5, SHA-1/256/384/512, SHA-3, and BLAKE2b. Users can upload files to verify their integrity or compare to other files.
